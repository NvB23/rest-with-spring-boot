spring:
  application:
    name: restwithspringboot
  output:
    ansi:
      enabled: ALWAYS
  devtools:
    restart:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false

  logging:
    level:
      root: ERRO
      # com.naum.rest_with_spring_boot: DEBUG